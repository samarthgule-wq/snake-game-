# Snake

Classic Snake (grid movement, food, score, game-over, restart).

## Run

1. `npm install` (no deps today, but keeps the flow consistent)
2. `npm run dev`
3. Open the URL printed in the terminal (defaults to `http://localhost:5173`)

## Controls

- Move: Arrow keys or WASD
- Pause/Resume: Space
- Restart: R

